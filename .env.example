# ===========================================
# Base de Datos
# ===========================================
DATABASE_URL="file:./prod.db"

# ===========================================
# YouTube API
# ===========================================
# Obtener en: https://console.cloud.google.com/ > APIs > YouTube Data API v3
YOUTUBE_API_KEY=tu_api_key_aqui

# ===========================================
# Cron Jobs
# ===========================================
# Secret para proteger endpoints de cron (generar con: openssl rand -base64 32)
# Usado por:
#   - /api/cron/sync (sincroniza videos de canales)
#   - /api/cron/audio-cleanup (limpia archivos de audio viejos)
CRON_SECRET=tu_secret_aleatorio_aqui

# Configurar en crontab (crontab -e):
# 0 */6 * * * curl -s -H "Authorization: Bearer $CRON_SECRET" https://tu-dominio.com/api/cron/sync
# 0 3 * * * curl -s -H "Authorization: Bearer $CRON_SECRET" https://tu-dominio.com/api/cron/audio-cleanup

# ===========================================
# URL Base (para sitemap y SEO)
# ===========================================
NEXT_PUBLIC_BASE_URL=https://unfeed.app

# ===========================================
# Autenticacion JWT
# ===========================================

# Secreto para firmar JWT (minimo 32 caracteres, generar con: openssl rand -base64 32)
AUTH_SECRET=tu_secreto_super_seguro_de_32_chars_minimo

# Duracion de la sesion en dias (opcional, default 7)
AUTH_SESSION_DAYS=7

# NOTA: Las credenciales de usuario se almacenan en la base de datos.
# Para crear el usuario inicial ejecuta:
#   bun run scripts/seed-user.ts <email> <password>

# ===========================================
# Audio Cache (Offline Mode)
# ===========================================

# Directorio para almacenar archivos de audio descargados
AUDIO_CACHE_DIR=./audio-cache

# Limite maximo de espacio en disco para cache de audio (en GB)
MAX_AUDIO_CACHE_GB=5

# ===========================================
# yt-dlp Configuration
# ===========================================

# Ruta al binario de yt-dlp (opcional, solo si no esta en PATH)
# YT_DLP_PATH=/usr/local/bin/yt-dlp

# Ruta al archivo de cookies de YouTube (opcional, para evitar rate limits)
# Exportar cookies con extension: https://chromewebstore.google.com/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc
# YT_DLP_COOKIES_PATH=/path/to/cookies_youtube.txt
